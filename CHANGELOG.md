# Changelog

## [Unreleased]

### Changed

- **Breaking:** Renamed the import package and PyPI distribution name from `notenotice` to `funnotice` to match the repository name. Anyone doing `import notenotice` or `pip install notenotice` must switch to `import funnotice` / `pip install funnotice`.
- The old `notenotice` PyPI package will receive one final release that forwards to `funnotice` (manual follow-up by the repo owner, not part of this change).
